# syntax=docker/dockerfile:1
# Git-backed Render wrapper around the pinned public Evolution release.
# The base image's startup instruction is intentionally inherited unchanged.
FROM docker.io/evoapicloud/evolution-api:v2.3.7

ENV SERVER_TYPE=http \
    SERVER_PORT=10000

EXPOSE 10000
